# Upgrade to FMTS v0.6.3

No database migration is required. Deploy the files and restart the service.
For an installed iPhone/Android PWA, fully close and reopen FMTS after deploy. If an old menu is still cached, open FMTS once in the browser and reload; the new service-worker cache version will replace the previous CSS.
